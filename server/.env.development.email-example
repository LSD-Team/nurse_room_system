# Email Service Configuration
EMAIL_SERVICE_URL=http://10.182.1.198/apis/lsd-smtp-service
APP_NAME=NURSE_ROOM_SYSTEM
NODE_ENV=development

# ⭐ TEST EMAIL OVERRIDE (for development/testing phase)
# When set and NODE_ENV is NOT 'production', ALL emails are sent to this address
# Once testing is complete and verified, remove or comment out this line to use actual emails
TEST_EMAIL_OVERRIDE=wayroad@shindengen.co.th

# Optional: Email service credentials (if needed)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-password
